{
  "name": "schedu-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "install-backend": "npm install --prefix backend",
    "postinstall": "npm run install-backend"
  }
}
