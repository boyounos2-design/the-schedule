rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow users to read their own data and admins to read everything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
